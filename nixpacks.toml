[phases.setup]
cmds = [
    "npm install",
    "npx prisma generate",
    "npx prisma db push || true",
    "npx prisma migrate dev || true"
]

[phases.build]
cmds = [
    "npm run prisma:seed",
    "npm run build"
]
